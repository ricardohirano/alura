//Criar uma função que recebe um número como parâmetro e retorna o dobro desse número.
let numero = prompt('escolha um numero')
function gerarDobro(numero){
    return 2*numero;
}
let resultado=gerarDobro(numero);
console.log(resultado);